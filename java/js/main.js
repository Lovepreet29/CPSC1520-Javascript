/*
 * user field
 * password field
 * button
  
 
 * add the event to the button 
 */

 const fakeuser="love";
 const fakepassword="4905";
 
 const cardHolder = document.querySelector('.user-name');
 const cardPassword = document.querySelector('.card-password');
 const cardNumber = document.querySelector('.card-number');
 const submitButton = document.querySelector('.verify-card');
 
 submitButton.addEventListener('click', function(){

    const card = (cardName.value);
    if (fakeuser == name) {
console.log("username is valid ")
    }
 else{
console.log("username is not valid")
 }
 })